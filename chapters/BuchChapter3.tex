\documentclass[../Bachelor_LennartKoliwer.tex]{subfiles}
\graphicspath{{\subfix{../images/}}}
\begin{document}

Die Mar\v{c}enko-Pastur-Verteilung ist ein Wahrscheinlichkeitsmaß, dass die asymptotische empirische Spektralverteilung der Eigenwerte von großen Stichproben-Kovarianzmatrizen beschreibt.
Das Ziel dieses Kapitels ist es zu zeigen, dass die empirische Spektralverteilung schwach gegen die Mar\v{c}enko-Pastur-Verteilung konvergiert.

Dieses Kapitel basiert auf dem Buch \glqq Spectral analysis of large dimensional random matrices\grqq\ von Zhidong Bai und Jack W. Silverstein \cite{BaiSilverstein}.




%---------------------------------------------------------------

\section{Mar\v{c}enko-Pastur-Momente}

Sei $X$ eine ${p\times n}$ große Matrix, dessen Elemente unabhängig und identisch verteilte Zufallsvariablen sind. 
Dabei sind ihre Erwartungswerte $0$ und ihre Varianz beträgt $\sigma^2$. 
Weiter sind $x_k=\left(x_{1k},\dots,x_{pk}\right)$, $\bm{X}=(x_1,\dots,x_n)$ und $\overline{x}=\frac1n\sum_{k=1}^n x_k$. 
Dann ist die Stichproben-Kovarianzmatrix
\begin{align}
    \bm{S}=\frac{1}{n}\sum_{k=1}^{n}(x_k-\overline{x})(x_k-\overline{x})^H\,. \label{eq:StichKovar}
\end{align}
Die Mar\v{c}enko-Pastur-Verteilung $F_y$ ist ein Wahrscheinlichkeitsmaß auf $(\RR,\mathcal{B}(\RR))$. Für $y,\sigma^2\in(0,\infty)$ und $x\in\RR$ 
sind 
\[a=\sigma^2\left(1-\sqrt{y}\right)^2\text{  und  }b=\sigma^2\left(1+\sqrt{y}\right)^2\] 
wobei $y=\frac{p}{n}$ das Dimonsions-Stichproben-Verhältnis ist. 
Die Dichtefunktion der Mar\v{c}enko-Pastur-Verteilung ist dann durch
\begin{align}
    p_y(x)=\begin{cases*}
        \dfrac{1}{2\pi x y \sigma^2}\sqrt{(b-x)(x-a)}\,, & wenn $a\leq x\leq b$ \\
        0\,, & \text{sonst}
    \end{cases*}
\end{align}
gegeben. 
Wenn $\sigma^2=1$ ist, dann sprechen wir von der Standard-M-P-Verteilung.
Die $k$-ten Momente der M-P-Verteilung sind durch
\begin{align}
    \beta_k=\beta_k\left(y,\sigma^2\right)\coloneqq\int_{a}^{b}x^kp_y(x)\diff x \label{eq:momente}
\end{align} 
definiert. 
Wir können die Momente der M-P-Verteilung durch ihren Standardfall beschreiben.
\begin{lemma}
    Für alle $k\geq1$ gilt
    \begin{align*}
        \beta_k\left(y,\sigma^2\right)=\sigma^{2k}\beta_k\left(y,1\right)\,.
    \end{align*}
\end{lemma}
\begin{proof}
    Nach Definition \eqref{eq:momente} haben wir 
    \begin{align*}
        \beta_k\left(y,\sigma^2\right)&=\int_{a}^{b}x^kp_y(x)\diff x\\
        &=\int_{\sigma^2\left(1-\sqrt{y}\right)^2}^{\sigma^2\left(1+\sqrt{y}\right)^2}x^k\frac{1}{2\pi x y \sigma^2}\sqrt{\left(\sigma^2\left(1+\sqrt{y}\right)^2-x\right)\left(x-\sigma^2\left(1-\sqrt{y}\right)^2\right)}\diff x\,. 
        \intertext{Substituieren wir mit $x=u\sigma^2$ erhalten wir}
        \beta_k\left(y,\sigma^2\right)&=\int_{\left(1-\sqrt{y}\right)^2}^{\left(1+\sqrt{y}\right)^2}\left(u\sigma^2\right)^k\frac{1}{2\pi u y \sigma^2}\sqrt{\left(\sigma^2\left(1+\sqrt{y}\right)^2-u\sigma^2\right)\left(u\sigma^2-\sigma^2\left(1-\sqrt{y}\right)^2\right)}\diff u\\
        &=\int_{\left(1-\sqrt{y}\right)^2}^{\left(1+\sqrt{y}\right)^2}u^k\sigma^{2k}\frac{1}{2\pi u y \sigma^2}\sqrt{\sigma^4\left(\left(1+\sqrt{y}\right)^2-u\right)\left(u-\left(1-\sqrt{y}\right)^2\right)}\diff u\\
        &=\sigma^{2k}\int_{\left(1-\sqrt{y}\right)^2}^{\left(1+\sqrt{y}\right)^2}u^k\frac{1}{2\pi u y }\sqrt{\left(\left(1+\sqrt{y}\right)^2-u\right)\left(u-\left(1-\sqrt{y}\right)^2\right)}\diff u\\
        &=\sigma^{2k}\beta_k\left(y,1\right)\,.
    \end{align*}
    Die Momente lassen sich also durch $\sigma^{2k}$ skaliert über die Standard-M-P-Verteilung beschreiben.
\end{proof}

Damit können wir dann zeigen, dass die Mar\v{c}enko-Pastur-Verteilung eindeutig durch die Momente ${\beta_x}$ bestimmt wird und wie die Momente explizit bestimmt aussehen.
%----------------------------------------------------------------------------------

\begin{lemma}[Carleman {\cite[509]{BaiSilverstein}}]  \label{lem:Carlman}
    Sei $\{\beta_k=\beta_k(F)\}_{k\in\NN_0}$ eine Folge von Momenten der Verteilungsfunktion $F$. Ist die \emph{Carleman-Bedingung}
    \begin{align*}
        \sum_{k=1}^{\infty} \beta_{2k}^{-\frac{1}{2k}}=\infty
    \end{align*}
    erfüllt, dann wird die Verteilung $F$ durch $\{\beta_k\}_{k\in\NN_0}$ eindeutig bestimmt.
\end{lemma}

Dass $\beta_k\left(y,\sigma^2\right)$ die Mar\v{c}enko-Pastur-Verteilung eindeutig bestimmt, lässt sich dann mit Lemma \ref{lem:Carlman} zeigen.
Wir sehen einmal nach Definition \ref{eq:momente}, dass wir $\beta_{2k}$ durch
\begin{align}
    \beta_{2k}  &= \frac{1}{2\pi y}\int_{a}^{b}x^{2k-1}\sqrt{(b-x)(x-a)}dx \nonumber\\
                &\leq \frac{1}{2\pi y}\int_{a}^{b}x^{2k-1}\sqrt{(b-a)(b-a)}dx \nonumber\\
                &= \frac{1}{2\pi y}\int_{a}^{b}x^{2k-1}(b-a)dx \nonumber\\
                &= \frac{1}{4k\pi y}(b^{2k}-a^{2k})(b-a) \nonumber\\
                &= \frac{1}{k\pi\sqrt{y}}(b^{2k}-a^{2k}) \nonumber\\
                &\leq \frac{1}{k\pi\sqrt{y}}b^{2k} \nonumber\\
                &\leq b^{2k} = \left(1+\sqrt{y}\right)^{4k} \label{eq:moment_abschätzen}
\end{align}
abschätzen können. Mit der Abschätzung \eqref{eq:moment_abschätzen} und Lemma \ref{lem:Carlman} haben wir durch
\begin{align*}
        \sum_{k=1}^{\infty} \beta_{2k}^{-\frac{1}{2k}} \leq \sum_{k=1}^{\infty} \left(1+\sqrt{y}\right)^{2} = \infty
\end{align*}
die Carleman-Bedingung erfüllt, womit wir gezeigt haben, dass die Mar\v{c}enko-Pastur-Verteilung durch die Momente $\beta_k$ eindeutig bestimmt ist.

\begin{lemma}
    Die explizite Darstellung der Momente ist 
    \begin{align}
        \beta_k = \sum_{r=0}^{k-1}\frac{1}{r+1}\binom{k}{r}\binom{k-1}{r}y^r\,.
    \end{align}
\end{lemma}


\begin{proof}
    Wir haben nach Definition \eqref{eq:momente}
    \begin{align*}
        \beta_k=\frac{1}{2\pi y}\int_{a}^{b}x^{k-1}\sqrt{(b-x)(x-a)}\diff x\,.
    \end{align*}
    Substituieren wir mit $x=1+y+z$ und setzen $a$ und $b$ ein, erhalten wir
    \begin{align*}
        \beta_k &= \frac{1}{2\pi y}\int_{-2\sqrt{y}}^{2\sqrt{y}}(1+y+z)^{k-1}\sqrt{\left(\left(1+\sqrt{y}\right)^2-1-y-z\right)\left(1+y+z-\left(1-\sqrt{y}\right)^2\right)}\diff z \\
        &= \frac{1}{2\pi y}\int_{-2\sqrt{y}}^{2\sqrt{y}}(1+y+z)^{k-1}\sqrt{4y-z^2}\diff z\,.
    \end{align*}
    Durch Einsetzen des Binomischen Lehrsatzes lässt sich das Integral zu 
    \begin{align*}
        \beta_k &= \frac{1}{2\pi y}\int_{-2\sqrt{y}}^{2\sqrt{y}}\sum_{\ell=0}^{k-1}\binom{k-1}{\ell}(1+y)^{k-1-\ell}z^\ell\sqrt{4y-z^2}\diff z \\
        &= \frac{1}{2\pi y}\sum_{\ell=0}^{k-1}\binom{k-1}{\ell}(1+y)^{k-1-\ell}\int_{-2\sqrt{y}}^{2\sqrt{y}}z^\ell\sqrt{4y-z^2}\diff z
    \end{align*}
    umformen.
    Um das Integral weiter zu vereinfachen, substituieren wir mit $z=2\sqrt{y}u$ und erhalten 
    \begin{align*}
        \beta_k &= \frac{1}{2\pi y}\sum_{\ell=0}^{k-1}\binom{k-1}{\ell}(1+y)^{k-1-\ell} \int_{-1}^{1}\left(2\sqrt{y}u\right)^\ell\sqrt{4y-4yu^2}2\sqrt{y}\diff u \\
        &= \frac{1}{2\pi y}\sum_{\ell=0}^{k-1}\binom{k-1}{\ell}(1+y)^{k-1-\ell} (2\sqrt{y})^\ell 4y \int_{-1}^{1}u^\ell\sqrt{1-u^2}\diff u\,.  
        \intertext{Hier ist unser Integrand ungerade, sofern $\ell$ ungerade ist. Da die Integralgrenzen symmetrisch um den Nullpunkt liegen ist das Integral für alle ungeraden $\ell$ gleich $0$. Dadurch ist jeder zweite Summand $0$. Wir können also jeden zweiten Summanden überspringen. Dafür lassen wir die Summe bis zur Hälfte von $k-1$ laufen und summieren über $2\ell$ statt $\ell$. Für den Fall, dass $k-1$ ungerade ist, runden wir ab, weil es dann nur $\frac{k-2}{2}$ Summanden gibt die ungleich $0$ sind. Wir haben also}
        \beta_k &= \frac{1}{2\pi y}\sum_{\ell=0}^{\left\lfloor\frac{k-1}{2}\right\rfloor}\binom{k-1}{2\ell}(1+y)^{k-1-2\ell} (2\sqrt{y})^{2\ell} 4y \int_{-1}^{1}u^{2\ell}\sqrt{1-u^2}\diff z\\
        &= \frac{1}{2\pi y}\sum_{\ell=0}^{\left\lfloor\frac{k-1}{2}\right\rfloor}\binom{k-1}{2\ell}(1+y)^{k-1-2\ell} (4y)^{\ell+1} \int_{-1}^{1}u^{2\ell}\sqrt{1-u^2}\diff z\,.
    \end{align*}
    Hier ist der Integrand eine gerade Funktion für alle $0\leq \ell$. Da die Integralgrenzen symmetrisch um den Nullpunkt liegen können wir auch zweimal über das halbe Intervall integrieren. Folglich gilt
    \begin{align*}
        \beta_k = \frac{1}{2\pi y}\sum_{\ell=0}^{\left\lfloor\frac{k-1}{2}\right\rfloor}\binom{k-1}{2\ell}(1+y)^{k-1-2\ell} (4y)^{\ell+1} 2\int_{0}^{1}u^{2\ell}\sqrt{1-u^2}\diff z\,.        
    \end{align*}
    Wenn wir jetzt mit $u=\sqrt{w}$ substituieren,
    \begin{align*}
        \beta_k &= \frac{1}{2\pi y}\sum_{\ell=0}^{\left\lfloor\frac{k-1}{2}\right\rfloor}\binom{k-1}{2\ell}(1+y)^{k-1-2\ell} (4y)^{\ell+1} \int_{0}^{1}w^{\ell}\sqrt{1-w}\frac{1}{\sqrt{w}}\diff w \\
        &= \frac{1}{2\pi y}\sum_{\ell=0}^{\left\lfloor\frac{k-1}{2}\right\rfloor}\binom{k-1}{2\ell}(1+y)^{k-1-2\ell} (4y)^{\ell+1} \int_{0}^{1}w^{\ell-\frac12}\sqrt{1-w}\diff w
    \end{align*}
    erhalten wir ein Integral, dass wir mittels Eulerschen Betafunktion \textcolor{red}{Quelle!} lösen können.
    Die Eulersche Betafunktion ist durch 
    \[
        \int_{0}^{1}t^{p-1}(1-t)^{q-1}dt = \frac{\Gamma(p)\Gamma(q)}{\Gamma(p+q)} 
    \]
    gegeben. Wir können also mit $p=\ell+\frac12$ und $q=1+\frac12$ das Integral durch die Gammafunktionen 
    \begin{align}
        \int_{0}^{1}w^{\ell-\frac12}\sqrt{1-w}\diff w &= \frac{\Gamma(\ell+\frac12)\Gamma(1+\frac12)}{\Gamma(\ell+2)} \nonumber \\
        &= \frac{1}{(\ell+1)!}\lr{\frac{(2\ell)!\sqrt{\pi}}{\ell!4^\ell}\frac{2!\sqrt{\pi}}{4}} \nonumber \\
        &= \frac{1}{(\ell+1)!}\lr{\frac{(2\ell)!}{\ell!4^\ell}\frac12\pi} \label{eq:gamma}
    \end{align}
    beschreiben.
    Nach Einsetzen von \eqref{eq:gamma} erhalten wir 
    \begin{align*}
        \beta_k &= \frac{1}{2\pi y}\sum_{\ell=0}^{\left\lfloor\frac{k-1}{2}\right\rfloor}\binom{k-1}{2\ell}(1+y)^{k-1-2\ell} (4y)^{\ell+1} \frac{1}{(\ell+1)!}\lr{\frac{(2\ell)!}{\ell!4^\ell}\frac12\pi} \\
        &= \sum_{\ell=0}^{\left\lfloor\frac{k-1}{2}\right\rfloor} \frac{1}{2\pi y} \frac{(k-1)!}{(2\ell)!(k-1-2\ell)!}(1+y)^{k-1-2\ell} (4y)^{\ell+1} \frac{1}{(\ell+1)!}\lr{\frac{(2\ell)!}{\ell!4^\ell}\frac12\pi}\,.
    \end{align*}

    Hier lassen sich diverse Variablen so kürzen, dass wir 

    \begin{align*}
        \beta_k = \sum_{\ell=0}^{\left\lfloor\frac{k-1}{2}\right\rfloor} \frac{(k-1)!}{\ell!(\ell+1)!(k-1-2\ell)!}y^\ell(1+y)^{k-1-2\ell}
    \end{align*}
    erhalten.
    Wenn wir den Binomischen Lehrsatz mit 
    \begin{align*}
        (1+y)^{k-1-2\ell} &= \sum_{s=0}^{k-1-2\ell}\binom{k-1-2\ell}{s}y^s\\
        &= \sum_{s=0}^{k-1-2\ell}\frac{(k-1-2\ell)!}{s!(k-1-2\ell-s)!}y^s
    \end{align*}
    einsetzen, haben wir 
    \begin{align*}
        \beta_k &= \sum_{\ell=0}^{\left\lfloor\frac{k-1}{2}\right\rfloor}\sum_{s=0}^{k-1-2\ell} \frac{(k-1)!}{\ell!(\ell+1)!(k-1-2\ell)!}y^\ell\frac{(k-1-2\ell)!}{s!(k-1-2\ell-s)!}y^s \\
        &= \sum_{\ell=0}^{\left\lfloor\frac{k-1}{2}\right\rfloor}\sum_{s=0}^{k-1-2\ell} \frac{(k-1)!}{\ell!(\ell+1)!s!(k-1-2\ell-s)!}y^{\ell+s}\,.
    \end{align*}

    Jetzt wollen wir den Term wieder vereinfachen. Dafür setzen wir $r=s+\ell$
    \begin{align*}
        \beta_k &= \sum_{\ell=0}^{\left\lfloor\frac{k-1}{2}\right\rfloor}\sum_{r=\ell}^{k-1-\ell} \frac{(k-1)!}{\ell!(\ell+1)!(r-\ell)!(k-1-\ell-r)!}y^{r}
    \end{align*}
    und erweitern mit zwei Einsen
    \begin{align*}
        \beta_k &= \sum_{\ell=0}^{\left\lfloor\frac{k-1}{2}\right\rfloor}\sum_{r=\ell}^{k-1-\ell} \frac{(k-1)!}{\ell!(\ell+1)!(r-\ell)!(k-1-\ell-r)!} \frac{r!}{r!}\frac{(k-r)!}{(k-r)!}y^{r} 
        \\
        &= \sum_{\ell=0}^{\left\lfloor\frac{k-1}{2}\right\rfloor}\sum_{r=\ell}^{k-1-\ell} \frac{(k-1)!}{r!(k-r)!} \frac{r!}{\ell!(\ell-r)!} \frac{(k-r)!}{(k-r-\ell-1)!(\ell+1)!} y^{r} 
        \\
        &= \sum_{\ell=0}^{\left\lfloor\frac{k-1}{2}\right\rfloor}\sum_{r=\ell}^{k-1-\ell} \frac1k\frac{k!}{r!(k-r)!} \frac{r!}{\ell!(\ell-r)!} \frac{(k-r)!}{(\ell+1)!(k-r-(\ell+1))!} y^{r} 
        \\
        &= \sum_{\ell=0}^{\left\lfloor\frac{k-1}{2}\right\rfloor}\sum_{r=\ell}^{k-1-\ell} \frac1k\binom{k}{r} \binom{r}{\ell} \binom{k-r}{\ell+1} y^{r}\,.
    \end{align*}
    Für den nächsten Schritt müssen wir die Laufvariablen voneinander unabhängig machen. Momentan haben wir erst das $\ell$ und ermitteln damit, inwieweit das $r$ eingeschränkt ist. Für ein festes aber beliebiges $k$ liegen unsere Laufvariablen in
    \begin{align*}
        A = \left\{(\ell,r)\in\NN^2 \,\middle|\, 0\leq \ell \leq \left\lfloor\frac{k-1}{2}\right\rfloor,\, \ell\leq r \leq k-1-\ell\right\}\,.
    \end{align*}
    Daraus wissen wir, dass \[0\leq \ell,\, r\leq k-1-\ell\] gilt, woraus wir \[\ell\leq k-1-r\] folgern können. Damit erhalten wir 
    \begin{align*}
                    &0\leq \ell\leq r \leq k-1-\ell\leq k-1 \\
        \implies    &0\leq r\leq k-1,\, 0\leq \ell\leq\min(r,k-1-r)
    \end{align*} 
    damit können wir also die Summen äquivalent über 
    \begin{align}
        A = \left\{(\ell,r)\in\NN^2 \,\middle|\, 0\leq r\leq k-1,\, 0\leq \ell\leq\min(r,k-1-r)\right\} \label{eq:laufvar}
    \end{align}
    laufen lassen.

    Wir haben also mit \eqref{eq:laufvar}
    \begin{align*}
        \beta_k &= \sum_{r=0}^{k-1}\sum_{\ell=0}^{\min(r,k-1-r)} \frac1k\binom{k}{r} \binom{r}{\ell} \binom{k-r}{\ell+1} y^{r}\\
        &= \sum_{r=0}^{k-1} \frac1k\binom{k}{r} y^{r}\sum_{\ell=0}^{\min(r,k-1-r)} \binom{r}{\ell} \binom{k-r}{\ell+1}\,.
    \end{align*}
    Das Minimum können wir noch weiter vereinfachen, da die Summanden gleich $0$ sind, sobald $\ell>r$ oder $\ell>k-1-r$ ist. Wenn wir die Summe also bis $r$ laufen lassen, ändern wir nichts am Ergebnis. 
    \begin{align*}
        \beta_k &= \sum_{r=0}^{k-1} \frac1k\binom{k}{r} y^{r}\sum_{\ell=0}^{r} \binom{r}{\ell} \binom{k-r}{\ell+1}\,.
    \end{align*}
    An diesem Punkt müssen wir nur noch die innere Summe vereinfachen. Dafür machen wir einen Indexshift und formen die Binomialkoeffizienten etwas um. Wir haben also
    \begin{align*}
        \beta_k &= \sum_{r=0}^{k-1} \frac1k\binom{k}{r} y^{r}\sum_{\ell=1}^{r+1} \binom{r}{\ell-1} \binom{k-r}{\ell} \\
        &= \sum_{r=0}^{k-1} \frac1k\binom{k}{r} y^{r}\sum_{\ell=1}^{r+1} \binom{r}{r-(\ell-1)} \binom{k-r}{\ell} \\
        &= \sum_{r=0}^{k-1} \frac1k\binom{k}{r} y^{r}\sum_{\ell=1}^{r+1} \binom{r}{(r+1)-\ell} \binom{k-r}{\ell}\,.
    \end{align*}
    Wir wollen hier die Vandermonde Identität \textcolor{red}{Quelle!} 
    \begin{align}
        \sum_{"o=0}^{"u}\binom{n}{"o}\binom{m}{"u-"o}=\binom{n+m}{"u} \label{eq:vandermonde}
    \end{align}
    nutzen. Dafür lassen wir die Summe von $\ell=0$ beginnen, da der Summand das Ergebnis nicht ändert. Mit der Identität \eqref{eq:vandermonde} erhalten wir
    \begin{align*}
        \beta_k &= \sum_{r=0}^{k-1} \frac1k\binom{k}{r} y^{r}\sum_{\ell=0}^{r+1} \binom{r}{(r+1)-\ell} \binom{k-r}{\ell}\\
        &= \sum_{r=0}^{k-1} \frac1k\binom{k}{r}\binom{k}{r+1} y^{r}\\
        &= \sum_{r=0}^{k-1} \frac1k\frac{r+1}{r+1}\binom{k}{r}\binom{k}{r+1} y^{r} \\
        &= \sum_{r=0}^{k-1} \frac{1}{r+1}\binom{k}{r}\binom{k-1}{r} y^{r}\,.
    \end{align*}
\end{proof}




%--------------------------------------------------------------------------







\section{Vorbereitung}

Da wir uns im Bereich der großen Zufallsmatrizen befinden, verwenden wir für die Spektralanalyse die empirische Spektralverteilung.

\begin{definition}\label{def:ESD}
    Sei $S$ eine ${p\times p}$ große hermitische Matrix mit Eigenwerten $\lambda_i$ mit $i=1,\dots,p$ dann ist die \emph{empirische Spektralverteilung} durch
    \begin{align*}
        F^S(x)=\frac1p|A| \quad \text{mit } A\coloneqq\{i\leq p \colon \lambda_i \leq x\}
    \end{align*}
    definiert.
\end{definition}

Diese Verteilung sagt aus, wie die Singulärwerte einer Zufallsmatrix verteilt sind.
Was uns interessiert, ist was passiert, wenn die Dimensionen gegen unendlich laufen.
Dafür verwenden wir die Stichproben-Kovarianzmatrix \eqref{eq:StichKovar} und analysieren ihr Verhalten im unendlich Dimensionalen.
Durch folgenden Satz können wir die Stichproben-Kovarianzmatrix noch weiter vereinfachen.

\begin{theorem}\label{theo:ESDrang1}
    Seien $A,B\in\CC^{p\times n}$. Dann gilt 
    \begin{align*}
        \dabs{F^{AA^H}-F^{BB^H}}\leq\frac1p\rang(A-B)\,.
    \end{align*}
\end{theorem}

Damit können wir sehen, dass Rang-$1$ Matrizen keinen Einfluss auf die Konvergenz der empirische Spektralverteilung haben. Wir können die Stichproben-Kovarianzmatrix 

\begin{align*}
    \bm{S}  &=  \frac{1}{m}\sum_{k=1}^{m}(x_k-\overline{x})(x_k-\overline{x})^H \\
            &=  \frac{1}{m}\lr{\sum_{k=1}^{m}x_kx_k^H-\sum_{k=1}^{m}x_k\overline{x}^H-\sum_{k=1}^{m}x_k^H\overline{x}+\sum_{k=1}^{m}\overline{x}\,\overline{x}^H} \\
            &=  \frac{1}{m}\lr{\sum_{k=1}^{m}x_kx_k^H - m\overline{x}\,\overline{x}^H}
\end{align*}

zu 

\begin{align}
    \bm{S}=\frac{1}{m}\sum_{k=1}^{m}x_kx_k^H \label{eq:StichKovarEinfach}
\end{align}

vereinfachen, da $\overline{x}\,\overline{x}^H$ eine Rang-$1$ Matrix ist, welche wir nach Satz \ref{theo:ESDrang1} vernachlässigen können.


%Das Ziel dieses Kapitels, ist es zu zeigen, dass die empirische Spektralverteilung gegen die Mar\v{c}enko-Pastur-Verteilung konvergiert. 
Im Beweis über die Konvergenz benutzen wir Elemente aus der Graphentheorie, die wir vorher noch einführen wollen.
\newline

Wir definieren uns also eine Klasse von gerichteten bipatiten Graphen mit folgenden Eigenschaften.

Seien $\emph{\textbf{i}}=(i_1,\dots,i_k)$ $k$ I-Knoten welche Werte zwischen $0$ und $p$ annehmen können und $\emph{\textbf{j}}=(j_1,\dots,j_k)$ $k$ J-Knoten dessen Werte zwischen $0$ und $n$ liegen. 
Unterschiedliche I/J-Knoten können den gleichen Wert annehmen, also sind sie koinzident. 
Die Kanten laufen von Knoten $i_m$ zu $j_m$ und von $j_m$ zu $i_{m+1}$ mit $m=1,\dots,k$ und der Konvention, dass $i_1=i_{k+1}$ gilt.
Wir bezeichnen $r$ als Anzahl nicht koinzidenter \emph{\textbf{i}} Knoten $-1$, $s$ als Anzahl nicht koinzidenter \emph{\textbf{j}} Knoten und $k$ als die Anzahl der Kanten die auf \emph{\textbf{i}} Knoten zeigen.
Diese Graphen Bezeichnen wir dann als $\Delta(k,r,s)$.
Für den Beweis teilen wir die Graphen in drei Kategorien ein.
 
Die Kategorie $\Delta_1(k,r,s)$  fasst alle Graphen zusammen, die zu jeder Kante von \emph{\textbf{i}} nach \emph{\textbf{j}} genau eine Kante vom gleichen \emph{\textbf{j}} Knoten zum gleichen \emph{\textbf{i}} Knoten haben. Hier gilt $r+s=k$.

Die Kategorie $\Delta_2(k,r,s)$ beinhaltet alle Graphen, die mindestens einmal zwischen zwei Knoten genau eine Kante besitzen. Also Graphen die mindestens eine Einzelkante besitzen.

Und die letzte Kategorie $\Delta_3(k,r,s)$ sind alle Graphen die weder in $\Delta_1(k,r)$ noch in $\Delta_2(k,r,s)$ gehören.
\begin{figure}[H]
    \centering
    \includegraphics[width=12cm]{Graphen_Beispiel_Platzhalter}
    \caption*{Beispiel für Graphen}\label{fig:GraphenBsp}
\end{figure}
Für bessere Lesbarkeit schreiben wir nur noch $\Delta_{1,2,3}$.
Wir bezeichnen zwei Graphen als isomorph, wenn wir durch eine geeignete Zuweisung von \emph{\textbf{i}}/\emph{\textbf{j}} Knoten gibt, mit der die Graphen identisch sind. 
Für diese Graphen haben wir noch ein paar Lemmata.

\begin{lemma}[{\cite[43]{BaiSilverstein}}]\label{lem:AnzahlIsomklasse}
    Die Anzahl der Graphen in jeder Isomorphismenklasse von $\Delta(k,r,s)$ Graphen, mit festen $k$, $r$ und $s$ ist
    \begin{align*}
        \alpha = p(p-1)\cdots(p-r)n(n-1)\cdots(n-s+1)=p^{r+1}n^s\lr{1+\mathcal{O}\lr{\frac1n}}\,.
    \end{align*}
\end{lemma}

\begin{lemma}[{\cite[43]{BaiSilverstein}}]\label{lem:AnzahlNichtKoinzidenterKnotenKateg3}
    Die Anzahl nicht koinzidenter Knoten von $\Delta_3(k,r,s)$ Graphen also $r+s$ ist kleiner gleich $k$. 
\end{lemma}

\begin{lemma}[{\cite[43]{BaiSilverstein}}]\label{AnzahlKateg1Graphen}
    Die Anzahl nicht-isomorpher $\Delta_1(k,r,s)$ Graphen für festes k und r beträgt
    \begin{align*}
        \frac{1}{r+1}\binom{k}{r}\binom{k-1}{r}\,.
    \end{align*}
\end{lemma}



\section{Mar\v{c}enko-Pastur-Verteilung}


\begin{theorem}\label{theo:MPVert}
    Seien $\{x_{ij}\}$ unabhängig und identisch verteilte Zufallsvariablen mit Erwartungswert $0$ und Varianz $\sigma^2$. Sei weiter $\frac{p(n)}{n}\overset{n\to\infty}{\longrightarrow}y \in(0,\infty)$. Dann konvergiert $F^S$ fast sicher gegen 
    \begin{align}
    p_y(x)=\begin{cases*}
        \dfrac{1}{2\pi x y \sigma^2}\sqrt{(b-x)(x-a)}\,, & wenn $a\leq x\leq b$ \\
        0\,, & \text{sonst}\,.
    \end{cases*}
\end{align}
\end{theorem}

Im folgenden Beweis nehmen wir o.B.d.A an, dass die Varianz $\sigma^2=1$ ist, wir also den Standardfall betrachten. 
Um die Konvergenz der empirischen Spektralverteilung \ref{def:ESD} gegen die Mar\v{c}enko-Pastur-Verteilung zu zeigen, müssen wir erstmal sicherstellen, dass die Zufallsvariablen $x_{ij}$ die notwendigen Bedingungen erfüllen damit wir die Momentmethode benutzen können. 
Dafür müssen wir einmal die Zufallsvariablen gleichmäßig beschränken, damit wir keine unendlich großen Eigenwerte haben und entsprechend die Erwartungswerte wieder zentralisieren also auf $0$ setzen und wir Reskalieren um die Varianz zu erhalten.


\begin{proof}
    Sei $C\in\RR_+$. Weiter seien 
    \begin{align*} 
        \hat{x}_{ij} = x_{ij}\mathbf{1}(|x_{ij}| \leq C)\,,
        &\quad
        \tilde{x}_{ij} = \hat{x}_{ij} - \mathbb{E}(\hat{x}_{11}) \\
        \hat{x}_i =
        \left(\hat{x}_{i1}, \dots, \hat{x}_{ip}\right)^{\top}\,,
        &\quad
        \tilde{x}_i =
        \left(\tilde{x}_{i1}, \dots, \tilde{x}_{ip}\right)^{\top} \\
        \hat{S}_n
        = \frac{1}{n} \sum_{i=1}^n \hat{x}_i \hat{x}_i^{H}
        = \frac{1}{n}\hat{X}_n \hat{X}_n^{H}\,,
        &\quad
        \tilde{S}_n
        = \frac{1}{n} \sum_{i=1}^n \tilde{x}_i \tilde{x}_i^{H}
        = \frac{1}{n}\tilde{X}_n \tilde{X}_n^{H}\,.
    \end{align*}
    Wir bezeichnen ab hier alle $X_n$ als $X$.
    Mit $\hat{S}_n$ und $\tilde{S}_n$ haben wir die beschränkte und zentralisierte Stichproben Kovarianzmatrix. Im Folgenden wollen wir zeigen, dass sich durch Beschränken und Zentralisieren der Grenzwert nicht ändert.
    Nach Lemma \ref{lem:L4} erhalten wir
    \begin{align*}
        \mathscr{L}^4\lr{F^{S_n},F^{\hat{S}_n}} &\leq \frac{2}{np}\tr\lr{XX^H+\hat{X}\hat{X}^H}\tr{\lr{(X-\hat{X})(X-\hat{X})^{H}}} \\
        &=\lr{\frac{2}{np}\sum_{i,j}x_{ij}x_{ij}^H+\hat{x}_{ij}\hat{x}_{ij}^H}\lr{\frac{1}{np}\sum_{i,j}(x_{ij}-\hat{x}_{ij})(x_{ij}-\hat{x}_{ij})^H} \\
        &=\lr{\frac{2}{np}\sum_{i,j}|x_{ij}|^2+|\hat{x}_{ij}|^2}\lr{\frac{1}{np}\sum_{i,j}|x_{ij}-\hat{x}_{ij}|^2} \\
        &\leq \lr{\frac{4}{np}\sum_{i,j}|x_{ij}|^2}\lr{\frac{1}{np}\sum_{i,j}|x_{ij}|^2\mathbf{1}(|x_{ij}| > C)} \\
        &\overset{f.s.}{\longrightarrow} 4\mathbb{E}\lr{|x_{ij}|^2}\mathbb{E}\lr{|x_{ij}|^2\mathbf{1}(|x_{ij}| > C)} \\
        &\overset{f.s.}{\longrightarrow} 4\mathbb{E}\lr{|x_{ij}|^2\mathbf{1}(|x_{ij}| > C)} \\
        &\overset{C\to\infty}{\longrightarrow} 0
    \end{align*}
    Der Abstand der empirischen Spektralverteilung und ihrer beschränkten Version ist also für ein genügend großes $C$ vernachlässigbar klein.
    Nach Satz \ref{theo:ESDrang1}
    \begin{align*}
        \dabs{F^{\hat{S}_n}-F^{\tilde{S}_n}} &\leq \frac1p \rang(\hat{X}_n-\tilde{X}_n) \\
        &= \frac1p \rang(\mathbb{E}(\hat{x}_{11})) \\
        &= \frac1p
    \end{align*}
    können wir sehen, dass das Entfernen des Erwartungswertes auch keinen Einfluss auf die Konvergenz hat.
    
    Wir haben jetzt die Verteilung so modifiziert, dass die Zufallsvariablen nicht mehr unendlich groß werden können und den Erhalt des Erwartungswertes garantiert, ohne die Konvergenz zu beeinträchtigen. 
    Um die letzte Bedingung, also den erhalt der Varianz, zu erfüllen müssen wir zeigen, dass durch Skalierung von $\tilde{S}_n$ mit $\tilde{\sigma}^2=\mathbb{E}\lr{|\tilde{x}_{ij}|^2}\to 1$, wenn $C\to \infty$ gilt, der Abstand zu $\tilde{S}_n$ wieder vernachlässigbar klein wird.

    Es gilt wieder mit Lemma \ref{lem:L4}
    \begin{align*}
        \mathscr{L}^4\lr{F^{\tilde{S}_n},F^{\tilde{\sigma}^{-2}\tilde{S}_n}} &\leq \frac{2}{n}\tr\lr{\tilde{X}\tilde{X}^H+\frac{1}{\tilde{\sigma}^2}\tilde{X}\tilde{X}^H}\tr{\lr{\lr{1-\frac{1}{\tilde{\sigma}}}^2 \tilde{X}\tilde{X}^H}} \\
        &= \lr{\frac{2}{np}\sum_{i,j}\tilde{x}_{ij}\tilde{x}_{ij}^H+\frac{\tilde{x}_{ij}\tilde{x}_{ij}^H}{\tilde{\sigma}^2}}\lr{\frac{1}{np}\sum_{i,j}\tilde{x}_{ij}\tilde{x}_{ij}^H-\frac{2\tilde{x}_{ij}\tilde{x}_{ij}^H}{\tilde{\sigma}}+\frac{\tilde{x}_{ij}\tilde{x}_{ij}^H}{\tilde{\sigma}^2}} \\
        &= \lr{\frac{2}{np}\sum_{i,j}|\tilde{x}_{ij}|^2+\frac{|\tilde{x}_{ij}|^2}{\tilde{\sigma}^2}}\lr{\frac{1}{np}\sum_{i,j}|\tilde{x}_{ij}|^2-\frac{2|\tilde{x}_{ij}|^2}{\tilde{\sigma}}+\frac{|\tilde{x}_{ij}|^2}{\tilde{\sigma}^2}} \\
        &= \lr{\frac{2}{np}\sum_{i,j}\frac{\tilde{\sigma}^2|\tilde{x}_{ij}|^2+|\tilde{x}_{ij}|^2}{\tilde{\sigma}^2}}\lr{\frac{1}{np}\sum_{i,j}\frac{\tilde{\sigma}^2|\tilde{x}_{ij}|^2-\tilde{\sigma}2|\tilde{x}_{ij}|^2+|\tilde{x}_{ij}|^2}{\tilde{\sigma}^2}} \\
        &= 2\lr{\frac{\tilde{\sigma}^2+1}{np\tilde{\sigma}^2}\sum_{i,j}|\tilde{x}_{ij}|^2}\lr{\frac{(\tilde{\sigma}-1)^2}{np\tilde{\sigma}^2}\sum_{i,j}|\tilde{x}_{ij}|^2} \\
        &\overset{f.s.}{\longrightarrow} 2\frac{\tilde{\sigma}^2+1}{\tilde{\sigma}^2}\mathbb{E}\lr{|\tilde{x}_{ij}|^2}\frac{(\tilde{\sigma}-1)^2}{\tilde{\sigma}^2}\mathbb{E}\lr{|\tilde{x}_{ij}|^2} \\
        &\overset{C\to\infty}{\longrightarrow} 0\,.
    \end{align*}
    Damit haben wir alle Bedingungen erfüllt. Für einen besseren Überblick verwenden wir weiterhin die Notation von $S_n$ und $X$, meinen aber die modifizierten Matrizen. 
    Wir haben also eine gleichmäßig durch $C$ beschränkte Zufallsvariable $x_{ij}$ mit Erwartungswert $0$ und Varianz $1$. 
    
    Nach der Momentmethode (S.10) erhalten wir
    \begin{align*}
        \beta_k(S_n)&= \int_{-\infty}^{\infty}x^kF^{S_n}\diff x \\
                    &= \frac{1}{p}\tr\lr{S_n^k} \\
                    &= \frac{1}{pn^k}\tr\lr{\lr{XX^H}^k}\,.    
    \intertext{Da die Dimensionen von $X$ gegen unendlich laufen, läuft auch die Anzahl der Summanden der Spur gegen unendlich.
    Wir können die Spur aber auch als}
            \frac{1}{pn^k}\tr\lr{\lr{XX^H}^k} &= \frac{1}{pn^k}\sum_{\emph{\textbf{i}}}\sum_{\emph{\textbf{j}}}x_{i_1j_1}\bar{x}_{i_2j_1}x_{i_2j_2}\bar{x}_{i_3j_2}\cdots x_{i_kj_k}\bar{x}_{i_1j_k}
    \end{align*}
        darstellen. Hier sind $\bar{x}_{ij}$ die Einträge aus $X^H$ und damit komplex konjugiert. 
        Dabei sind $x_{i_1j_1}\bar{x}_{i_2j_1}\dots$ die Faktoren der einzelnen Summanden der Spur und wir Summieren über alle Permutationen von \emph{\textbf{i}} und \emph{\textbf{j}}. 
        Wenn wir über alle Permutationen summieren, können wir diese als oben definierte Graphen darstellen. Hier stellen $x_{ij}$ die Kanten von \emph{\textbf{i}}-Knoten zu \emph{\textbf{j}}-Knoten dar und $\bar{x}_{ij}$ Kanten von \emph{\textbf{j}}- zu \emph{\textbf{i}}-Knoten. Wir definieren dann
    \begin{align*}
        \frac{1}{pn^k}\sum_{\emph{\textbf{i}}}\sum_{\emph{\textbf{j}}}x_{i_1j_1}\bar{x}_{i_2j_1}x_{i_2j_2}\bar{x}_{i_3j_2}\cdots x_{i_kj_k}\bar{x}_{i_1j_k} & \coloneqq \frac{1}{pn^k} \sum_{\emph{\textbf{i}},\emph{\textbf{j}}} X_{\Delta(k,r,s)}\,.
    \end{align*}

    Die Konvergenz lässt sich jetzt zeigen, indem wir zeigen, dass der Erwartungswert $\EE(\beta_k(S_n))$ gleich der Momente der Mar\v{c}enko-Pastur-Verteilung ist und die Varianz gegen null geht.
    Damit hätten wir eine fast sichere Konvergenz der empirischen Spektralverteilung gegen die Mar\v{c}enko-Pastur-Verteilung.
    
    Fangen wir an mit dem Erwartungswert.
    Nach Definition gilt
    \begin{align*}
        \EE(\beta_k(S_n)) = \frac{1}{pn^k} \sum_{\emph{\textbf{i}},\emph{\textbf{j}}}\EE\lr{X_{\Delta(k,r,s)}}\,.
    \end{align*}
    Teilen wir die Graphen in ihre Kategorien ein, erhalten wir mit Lemma \ref{lem:AnzahlIsomklasse}
    \begin{align*}
        \EE(\beta_k(S_n)) = \frac{1}{pn^k} \lr{\overbrace{\sum_{\Delta_1} \alpha\EE\lr{X_{\Delta_1}}}^{W_1\coloneqq}+\overbrace{\sum_{\Delta_2} \alpha\EE\lr{X_{\Delta_2}}}^{W_2\coloneqq}+\overbrace{\sum_{\Delta_3} \alpha\EE\lr{X_{\Delta_3}}}^{W_3\coloneqq}}\,.
    \end{align*}

    Für $W_1$ erhalten wir nach Lemma \ref{lem:AnzahlIsomklasse} und Lemma \ref{lem:AnzahlNichtKoinzidenterKnotenKateg3}
    \begin{align}
            W_1 &= \frac{1}{n^k p}\sum_{\Delta_1}\frac{1}{r+1}\binom{k}{r}\binom{k-1}{r}p^{r+1}n^{k-r}\lr{1+\mathcal{O}\lr{\frac{1}{p}}}\EE\lr{X_{\Delta_1}}\nonumber
        \intertext{Da es zu jeder Kante genau eine Kante in die entgegengesetzte Richtung gibt, gilt $\EE\lr{|x_{\emph{\textbf{i}}\emph{\textbf{j}}}|^2}=1$. Wir können also schreiben}
            W_1 &= \frac{1}{n^k p}\sum_{\Delta_1}\frac{1}{r+1}\binom{k}{r}\binom{k-1}{r}p^{r+1}n^{k-r}\lr{1+\mathcal{O}\lr{\frac{1}{p}}} \nonumber\\
            &= \sum_{\Delta_1}\frac{1}{r+1}\binom{k}{r}\binom{k-1}{r}\frac{p^{r+1}n^{k-r}}{n^k p}\lr{1+\mathcal{O}\lr{\frac{1}{p}}} \nonumber\\
        &= \sum_{\Delta_1}\frac{1}{r+1}\binom{k}{r}\binom{k-1}{r}y^r\lr{1+\mathcal{O}\lr{\frac{1}{p}}}\nonumber \\
            &= \sum_{r=0}^{k-1}\frac{1}{r+1}\binom{k}{r}\binom{k-1}{r}y^r+\mathcal{O}\lr{\frac{1}{p}}\label{eq:Delta1}\,.
    \end{align}

    Für $W_2$ können wir sehen, dass 
    \begin{align}
        W_2 = \frac{1}{n^k p}\sum_{\Delta_2} \alpha\EE\lr{X_{\Delta_2}} = 0 \label{eq:Delta2}
    \end{align}
    gilt, da wir den Erwartungswert auf Kanten aufteilen können, und es in Kategorie $\Delta_2$ mindestens eine Einzelkante gibt. Es gibt demnach mindestens ein $\omega_i$ in 
    \begin{align*}
        \EE\lr{X_{\Delta_2}}= \EE(x_{i_1j_1}^{\omega_1})\EE(x_{i_2j_1}^{\omega_2})\cdots
    \end{align*}
    welches gleich $1$ ist, woraus folgt, dass $\EE(x_{i_kj_k}^{1})=0=\EE\lr{X_{\Delta_2}}$ ist.

    Für $W_3$ erhalten wir wieder mit Lemma \ref{lem:AnzahlIsomklasse}, dass
    \begin{align*}
        W_3 = \frac{1}{n^k p}\sum_{\Delta_3} \alpha\EE\lr{X_{\Delta_3}}
    \end{align*}
    gilt.
    Weiter oben haben wir gezeigt, dass $x_{ij}$ gleichmäßig durch ein $C>0$ beschränkt ist. Daraus erhalten wir, dass $|X_{\Delta(k,r,s)}|\leq C^{2k}$ gilt. Mit Lemma \ref{lem:AnzahlEinezlKantenKateg3} wissen wir, dass die Anzahl nicht koinzidenter Knoten maximal $k$ betragen kann. Dadurch ist die Anzahl unterschiedlicher $\Delta_3$ Graphen in $\mathcal{O}\lr{n^k}$. Wir haben also
    \begin{align}
        \frac{1}{n^k p}\sum_{\Delta_3} \alpha\lr{X_{\Delta_3}} &= \frac{C^{2k}}{n^k p} \mathcal{O}\lr{n^k} \nonumber\\
        &= \mathcal{O}\lr{\frac{1}{p}} \label{eq:Delta3}\,.
    \end{align}
    Mit \eqref{eq:Delta1}, \eqref{eq:Delta2} und \eqref{eq:Delta3} erhalten wir dann
    \begin{align}
        \EE(\beta_k(S_n)) &= \sum_{r=0}^{k-1}\frac{1}{r+1}\binom{k}{r}\binom{k-1}{r}y^r+\mathcal{O}\lr{\frac{1}{p}} \nonumber\\
                        &=\beta_k(y,\sigma^2)+o(1) \label{eq:Erw}
    \end{align}

    Jetzt fehlt noch die Varianz.
    Diese ist nach Definition
    \begin{align*}
        \text{Var}(\beta_k(S_n)) &\coloneqq \frac{1}{pn^k} \sum_{\emph{\textbf{i}},\emph{\textbf{j}}} \lr{\EE\lr{X_{\Delta_a(k,r,s)}X_{\Delta_b(k,r,s)}}-\EE\lr{X_{\Delta_a(k,r,s)}}\EE\lr{X_{\Delta_b(k,r,s)}}}\,.
    \end{align*}
    Dabei sind $\Delta_a$ und $\Delta_b$ verschiedene Permutationen \emph{\textbf{i}} und \emph{\textbf{j}}. Wie für den Erwartungswert suchen wir uns Kategorien, um die Varianz stückweise zu beschreiben.

    Als Erstes betrachten wir alle Graphen $\Delta_a$ und $\Delta_b$ so, dass $\Delta_a$ und $\Delta_b$ unabhängig sind, sich also keine Kanten teilen.
    Daraus folgt direkt durch Rechenregeln von Erwartungswerten, dass
    \begin{align*}
        \EE\lr{X_{\Delta_a(k,r,s)}X_{\Delta_b(k,r,s)}} = \EE\lr{X_{\Delta_a(k,r,s)}}\EE\lr{X_{\Delta_b(k,r,s)}}
    \end{align*}
    gilt. Das heißt, für unabhängige Graphen ist die Varianz gleich $0$.

    Als Nächstes betrachten wir alle Graphen $\Delta_a\cup\Delta_b$, die mindestens eine Einzelkante besitzen. Damit erhalten wir durch $\Delta_2$ Eigenschaften wieder 
    \begin{align*}
        \EE\lr{X_{\Delta_a(k,r,s)}X_{\Delta_b(k,r,s)}} = 0 = \EE\lr{X_{\Delta_a(k,r,s)}}\EE\lr{X_{\Delta_b(k,r,s)}}\,.
    \end{align*}

    In allen anderen Möglichkeiten haben die Graphen keine Einzelkante und $\Delta_a$ und $\Delta_b$ teilen sich Kanten. Analog zu $W_3$ können wir sagen, dass die Anzahl möglicher Graphen in $\mathcal{O}(n^k)$ liegt. Da wir zwei Graphen haben ist die Anzahl also in $\mathcal{O}(n^{2k})$.
    Durch die gleichmäßige Beschränkung von $x_{ij}$ durch $C$ können wir die Varianz durch
    \begin{align}
        \text{Var}(\beta_k(S_n)) &\leq \frac{1}{p^2n^{2k}}C\mathcal{O}(n^{2k}) \nonumber\\
        &= \mathcal{O}\lr{\frac{1}{p^2}} \label{eq:Var}
    \end{align}
    abschätzen.

    Mit dem Erwartungswert \eqref{eq:Erw} und der Varianz \eqref{eq:Var} können wir darauf schließen, dass die empirische Spektralverteilung fast sicher gegen die Mar\v{c}enko-Pastur-Verteilung konvergiert.
\end{proof}





\end{document}



Ziel:
    \begin{align*}
        \EE(\beta_k(S_n)) &\coloneqq \frac{1}{pn^k} \sum_{\emph{\textbf{i}},\emph{\textbf{j}}}\EE\lr{X_{\Delta(k,r,s)}}\\
        &= \sum_{r=0}^{k-1}\frac{1}{r+1}\binom{k}{r}\binom{k-1}{r}y^r+\mathcal{O}\lr{\frac{1}{p}}
        \intertext{und}
        \text{Var}(\beta_k(S_n)) &\coloneqq \frac{1}{pn^k} \sum_{\emph{\textbf{i}}_1,\emph{\textbf{j}}_1,\emph{\textbf{i}}_2,\emph{\textbf{j}}_2,}\lr{\EE\lr{X_{G_1(\emph{\textbf{i}}_1,\emph{\textbf{j}}_1)}X_{G_2(\emph{\textbf{i}}_2,\emph{\textbf{j}}_2)}}-\EE\lr{X_{G_1(\emph{\textbf{i}}_1,\emph{\textbf{j}}_1)}}\EE\lr{X_{G_2(\emph{\textbf{i}}_2,\emph{\textbf{j}}_2)}}} \\ 
        &=\mathcal{O}\lr{\frac{1}{p^2}}
    \end{align*}